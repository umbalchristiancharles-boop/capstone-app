# TODO

- [x] Investigate current Apply Now modal markup and existing CSS classes.
- [x] Redesign ONLY the Job Application modal UI/UX markup in `resources/js/components/index.vue` (premium header, 2-column responsive layout, better spacing, modern upload areas, improved consent + buttons) WITHOUT changing submit/validation logic.
- [ ] Update modal styling in `resources/css/index.css` to match Chikin Tayo premium blue/yellow/white glassmorphism theme (inputs, upload boxes, gradients, animations, responsive behavior).
- [x] Re-check for template correctness (no broken tags) and run build/tests (if configured) to confirm no template errors.





